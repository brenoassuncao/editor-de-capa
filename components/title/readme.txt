Component of cover editor for sites.

This component is a template of a peice of site. the content will be dinamic but the layout is provided by this component.
you need to create a file called 'component.js' and create a function with the same name of the folder. implements two methods getHtml() ang build().
